@model IEnumerable<PortfolioManagementSystem.Models.Stocks>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StockName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StockName)
            </td>
            <td>
                <a class="btn btn-primary mx-1" asp-action="Edit" asp-route-id="@item.StockId">Edit</a> |
                <a class="btn btn-primary mx-1" asp-action="Details" asp-route-id="@item.StockId">Details</a> |
                <a class="btn btn-danger mx-1" asp-action="Delete" asp-route-id="@item.StockId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
